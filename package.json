{
  "name": "basique-project",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "",
    "dev": "",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "showdown": "^2.1.0"
  }
}
